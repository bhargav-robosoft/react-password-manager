import React from "react";

import loadingImg from "../../assets/icons/loading.gif";

const LoadingImg = (): JSX.Element => {
  return <img height={20} width={20} src={loadingImg} alt="Loading" />;
};

export default LoadingImg;
